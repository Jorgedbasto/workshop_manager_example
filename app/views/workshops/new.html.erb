<h1>New workshop</h1>

<% form_for(@workshop) do |f| %>
  <%= f.error_messages %>

  <%= render :partial=>"workshop_fields", :locals=>{:f=>f}%>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', workshops_path %>
